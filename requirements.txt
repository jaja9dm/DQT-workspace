# ── HTTP ────────────────────────────────────────
requests>=2.32

# ── Claude API ───────────────────────────────────
anthropic>=0.40

# ── 환경변수 ─────────────────────────────────────
python-dotenv>=1.0

# ── 국내 데이터 ──────────────────────────────────
finance-datareader>=0.9
pandas>=2.2
# pandas-ta: Python 3.10+ 필요. 미설치 시 수동 계산 폴백 자동 적용
# pandas-ta>=0.3

# ── 글로벌 데이터 ─────────────────────────────────
yfinance>=0.2
fredapi>=0.5

# ── 스케줄러 ─────────────────────────────────────
APScheduler>=3.10

# ── WebSocket (KIS 실시간 구독) ───────────────────
websocket-client>=1.6

# ── 문서 빌드 (build_html.py) ────────────────────
markdown>=3.6
