{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Edit|Write",
        "hooks": [
          {
            "type": "command",
            "command": "jq -r '.tool_input.file_path // \"\"' | { read -r f; echo \"$f\" | grep -q \"concept\\.md\" && cd /Users/dean/Documents/workspace-DQT && python3 scripts/build_html.py; } 2>/dev/null || true",
            "timeout": 30,
            "statusMessage": "concept.html 동기화 중..."
          }
        ]
      }
    ]
  }
}
